server {
  listen 443 ssl;
  # deprecated protocols (flagged) AND a modern one (TLS 1.3 must NOT be flagged — the scanner knows the difference)
  ssl_protocols TLSv1.0 TLSv1.2 TLSv1.3;
  ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256;   # ECDHE + RSA = quantum-broken; AES-128 = quantum-weakened
}
